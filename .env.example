# Leave empty in development: requests go through the Vite proxy in vite.config.ts,
# which avoids CORS entirely. Set to the full host for a production build, e.g.
# VITE_API_BASE=https://pinlon-lpms.bimats.com
VITE_API_BASE=
